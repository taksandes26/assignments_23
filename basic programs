#1. Write a program to iterate the first 10 numbers and in each iteration, print the sum of the current and previous number.
my_list = range(10)
new_list = list(my_list)
print(new_list)
sum = 0
previous_num = 0
for x in new_list:
    sum = x + previous_num
    print("current number is", x, " sum is ", sum)
    previous_num = x
# 2. Write a program to accept a string from the user and display characters that are present at an even index number.
input("i am larning python")
my_string = input("enter a string ")
for index, value in enumerate(my_string):
    print(index, value)
    if index % 2 == 0:
        print(value)

# 3. Check if the first and last number of a list is the same

given_numbers_x = [10,20,30,40,10]
given_numbers_y = [75,65,35,75,30]

def check_num(xyz):
 first_num = xyz[0]
 last_num = xyz[-1]
 if first_num == last_num:
    return True
 else:
    return False

 
m = check_num(given_numbers_x)
n = check_num(given_numbers_y) 
print(m)
print(n)
